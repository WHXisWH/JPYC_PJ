---
description: 前端分层架构与数据流规则
alwaysApply: true
---

# 分层架构规则

## 层级依赖（严格单向）

```
View → Controller (Hook) → Service → Model (Store)
                             ├──► @nodestay/api-client (HTTP)
                             └──► Indexed (IndexedDB → Polygon RPC)
```

## 各层职责边界

| 层 | 允许 | 禁止 |
|----|------|------|
| **View** (页面组件) | 调用 Controller Hook，纯渲染 | 直接调 Service/Store/API |
| **Controller** (Hook) | 只读 Store，调用 Service 方法 | 直接写 Store，直接调 HTTP/IndexedDB |
| **Service** | 拉数 → 转换 → 写入 Store | 依赖 React，感知 UI 状态 |
| **Model (Store)** | 被 Service 读写，被 Controller 只读 | 包含业务逻辑 |

## 多数据源规则

- 链上列表/聚合数据 → 必须进入前端 IndexedDB（Read Model）
- UI 默认读 IndexedDB；仅允许回退 RPC 单查补齐
- HTTP API 负责链下业务编排；链上负责可验证权益与资金流
- Store 为展示用缓存与 UI 状态，可清空重建

## 幂等键

checkout / purchasePass 等写操作必须通过 `idempotency-key` header 保证幂等，使用 `normalizeIdempotencyKey` 规范化。
